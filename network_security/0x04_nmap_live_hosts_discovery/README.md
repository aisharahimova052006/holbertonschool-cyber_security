Nmap Project
Bash scripts using nmap to scan IPs and networks, check ports, and discover hosts (ARP, ICMP, TCP, UDP).  
All scripts are 2 lines, start with `#, use `$1` as target, are executable, and follow Betty style.  
Goal: understand Nmap scanning and target enumeration on Kali Linux.
